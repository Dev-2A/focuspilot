# focuspilot env example
# 실제 사용 시: 이 파일을 복사해서 ".env"로 만들고 값만 수정하세요.
# (주의) .env는 커밋하지 않는 것을 권장합니다.

# 서버
HOST=127.0.0.1
PORT=8001
DEBUG=1

# 데이터베이스 (SQLite)
DB_PATH=focuspilot.db

# 앱 옵션 (선택)
APP_NAME=FocusPilot